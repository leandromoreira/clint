<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Clint enconder</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="css/bootstrap.css" rel="stylesheet">
    <link href='http://fonts.googleapis.com/css?family=Ubuntu' rel='stylesheet' type='text/css'>
    <style type="text/css">
      html,
      body {
        font-family: 'Ubuntu', sans-serif;
        height: 100%;
      }

      #wrap {
        min-height: 100%;
        height: auto !important;
        height: 100%;
        margin: 0 auto -60px;
      }

      #push,
      #footer {
        height: 60px;
      }
      #footer {
        background-color: #f5f5f5;
      }

      @media (max-width: 767px) {
        #footer {
          margin-left: -20px;
          margin-right: -20px;
          padding-left: 20px;
          padding-right: 20px;
        }
      }

      .container {
        width: auto;
        max-width: 680px;
      }
      .container .credit {
        margin: 20px 0;
      }

    </style>
    <link href="css/bootstrap-responsive.css" rel="stylesheet">
  </head>
  <body>
    <!-- Part 1: Wrap all page content here -->
    <div id="wrap">
      <!-- Begin page content -->
      <div class="container">
        <div class="page-header">
          <h1> ✰ Clint Enconder ✰ </h1>
        </div>
        <p class="lead">
          The open source encoder strongly based on FFmpeg.
        </p>
        <fieldset>
          <legend>▶ Setup Input</legend>
          <label for="device">Device</label>
          <select>
            <option>/dev/video0</option>
            <option>File</option>
          </select>

          <label for="device">Format</label>
          <select>
            <option>H.264</option>
          </select>
        </fieldset>
        <fieldset>
          <legend>◀ Setup Outup</legend>
          <label for="rtmp-name">RTMP address</label>
          <input id="rtmp-name" class="input-xxlarge" type="text" placeholder="rtmp://192.0.0.12/live"></input>

          <label for="stream-name">Stream name</label>
          <input id="stream-name" type="text" placeholder="live_event"></input>
        </fieldset>
        <button class="btn btn-large btn-primary">Stream</button>
      </div>

      <div id="push"></div>
    </div>
    <div id="footer">
      <div class="container">
        <p class="muted credit">CPU usage: 35% GPU usage: 10% Memory: 63%.</p>
      </div>
    </div>
    <script src="js/bootstrap.js"></script>
    <script src="js/knockout-2.2.1.js"></script>
  </body>
</html>

